# FinOS MCP Server — Enterprise integration
# Python 3.10+

# MCP SDK
mcp[cli]>=1.0.0

# Optional: use connectors for GL and draft (install from project root)
# pip install -e . or add connectors to path

# Webhook server (optional, run separately)
flask>=2.0.0
