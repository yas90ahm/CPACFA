{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Single source of truth for accounting validation rules (ASC/IFRS). Rounding tolerance and materiality.",
  "roundingTolerance": 0.01,
  "materiality": {
    "defaultThreshold": 0.01,
    "description": "Default materiality for balance checks and variance (e.g. 0.01 = 1 unit or 1%)"
  },
  "equations": {
    "debits_equal_credits": {
      "description": "Trial balance: Sum(Debits) == Sum(Credits)",
      "toleranceKey": "roundingTolerance"
    },
    "assets_equal_liabilities_plus_equity": {
      "description": "Balance sheet: Assets == Liabilities + Equity",
      "toleranceKey": "roundingTolerance"
    }
  }
}
