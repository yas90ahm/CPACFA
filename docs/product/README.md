# Product documents

These files describe the intended workflows and interface. They are inputs to the product, not proof that every feature is implemented.

Current behavior lives in the application and tests. When a product document and the code disagree, record the gap in `PROJECT_STATUS.md` instead of quietly changing the claim.
